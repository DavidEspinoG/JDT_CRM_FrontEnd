import SectionTitle from "../components/SectionTitle";

const Orders = () => {
    return (
        <>
            <SectionTitle>
                Orders
            </SectionTitle>
        </>
    )
};

export default Orders;